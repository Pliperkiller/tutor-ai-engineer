# f1.testing-pytest — apuntes

## Parte 1 (sesión 12, 2026-08-07)

### Por qué existe pytest
Verificabas la API con 8 `curl` a mano. Eso no se repite solo, nadie más lo
puede correr, y depende de que te acuerdes del caso raro (el 500 de S11 lo
encontraste de casualidad). Un test es ese curl escrito una vez y ejecutado
para siempre por algo que no se cansa.

### Las tres piezas

**El test**: función `test_*` con un `assert` pelado. Sin clases obligatorias,
sin `assertEqual`. pytest reescribe el assert para mostrarte los dos lados al
fallar.

**`TestClient`**: hace la request contra tu app **sin levantar servidor ni
tocar la red**. Sin puerto, sin `fastapi dev` en otra terminal.

```python
def test_health_returns_ok(client):
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}
```

**La fixture**: función que prepara lo que el test necesita, y que pytest llama
por ti. Es **el mismo patrón que `Depends` de FastAPI**: declaras, no llamas.
La diferencia es cómo se pide — en pytest, **el nombre del parámetro ES la
petición**.

```python
@pytest.fixture(autouse=True)
def reset_models_store():
    original = MODELS.copy()
    yield                    # <- pytest corre el test aquí
    MODELS.clear()           # <- teardown: corre aunque el test falle
    MODELS.update(original)
```

- Antes del `yield`: setup. Después: teardown.
- `autouse=True`: se aplica a todos sin que nadie la pida. Se usa para limpieza,
  cuando no te interesa recibir el objeto sino que el mundo quede limpio.
- Sin `yield` (con `return`) para las que no necesitan limpiar, como `client`.

### `conftest.py`
El archivo que pytest importa solo. Nombre exacto y obligatorio.
1. Fixtures compartidas **sin import** — los tests las piden por nombre.
2. Alcance por directorio y subdirectorios.
3. Corre **antes** que los archivos de test. Única ventana para preparar el
   mundo antes de que se importe el código bajo prueba.

### Generadores (lo que hace funcionar al `yield`)
- `yield` convierte la función en generador: **llamarla no ejecuta nada**, te
  devuelve un objeto congelado.
- `return` destruye el frame; `yield` lo **congela** (variables locales y
  posición siguen vivas).
- `next(g)` reanuda ese generador hasta el siguiente `yield`. `StopIteration`
  cuando se acaba. Un `for` es azúcar sobre `next()` + capturar `StopIteration`.
- Llamar la función **otra vez** crea un generador NUEVO, con frame propio.
  **Ese es todo el mecanismo del aislamiento entre tests**: pytest no hace
  `next()` sobre el generador viejo, llama la fixture de nuevo.
- Familia de `await`: funciones que se suspenden y se retoman. Las corrutinas de
  asyncio se construyeron históricamente sobre generadores.

### Identidad vs. contenido (el bug del día)
Copiaste el dict `MODELS` dentro de `conftest.py` en vez de importarlo. Tu
fixture limpiaba religiosamente **un objeto que nadie usaba**.

```python
d = {1: "a"}
otro = d
d = {}          # reasignar: mueve TU etiqueta. otro sigue con {1: 'a'}
d = otro
d.clear()       # mutar: cambia el objeto. otro SÍ lo ve
```

`main.py` tiene su propia referencia y nunca la vuelve a mirar. Por eso la
fixture **muta** (`.clear()` + `.update()`) en vez de reasignar. Y por eso
`from main import MODELS` es obligatorio: te ata al mismo objeto.

### El import ejecuta el módulo entero (P1, fallada)
Predijiste "fallarán los tests con dependencia de api key". Lo real: `main.py`
lee `os.environ["MODEL_REGISTRY_API_KEY"]` en la línea 42, **al importarse**.
`conftest.py` importa `main` → `KeyError` en la **colección**, antes de que
exista un solo test. No falla ninguno: **no corre ninguno**.

El arreglo va en `conftest.py`, físicamente encima del import:

```python
import os
# main.py lee la API key al importarse: la variable tiene que existir ANTES.
os.environ["MODEL_REGISTRY_API_KEY"] = "dev-secret-key"

from main import MODELS, app  # noqa: E402
```

Regla: un `noqa` nunca va solo, va con el comentario que explica por qué esta
línea es la excepción.

### Auditoría de mutación — el paso que casi nadie hace
**Un test verde no prueba nada hasta que lo ves ponerse rojo.** Rompes el código
a propósito, una rotura a la vez, y confirmas que la suite lo detecta.

| Rotura en `main.py` | Tests que cayeron |
|---|---|
| `404` → `200` en `get_model` | 2 |
| POST sin `dependencies=[...]` | 1 |
| `delete_model` sin `del MODELS[...]` | 1 |

Las tres detectadas: la suite es portante. Este es el antídoto exacto a los
asserts muertos de S7 (los que estaban dentro del `with pytest.raises` y nunca
se ejecutaban).

### Gestión de secretos (pregunta propia)
Lo del `conftest.py` **no es un secreto**: es un valor falso para un entorno
falso, y está bien que lo sea — la suite nunca debe tocar la key real.

Regla: **el código conoce el NOMBRE de la variable, nunca el VALOR.**

| Entorno | De dónde sale el valor |
|---|---|
| Tests | valor falso hardcodeado, en el repo |
| Dev local | `.env` gitignored, cargado con `pydantic-settings` |
| CI | secrets del repositorio inyectados como env vars |
| Producción | AWS Secrets Manager / SSM, Vault |

- `.env.example` **sí** se commitea: nombres y dummies, es la documentación de
  qué necesita el sistema para arrancar.
- `gitleaks`/`trufflehog` en pre-commit. Un secreto commiteado sigue vivo en la
  historia aunque borres la línea: la única respuesta seria es **rotar la key**.
- Fallar al arrancar si falta la variable es correcto. Lo peor es arrancar igual
  con un default.

### Dependencias: `--dev` o no
`uv add X` → `[project].dependencies`: lo que el **código** necesita para correr.
`uv add --dev X` → `[dependency-groups].dev`: lo que **tú** necesitas para
trabajar. El criterio no es el paquete, es **quién hace el import**: httpx es
runtime en el cliente async y dev aquí (solo lo usa `TestClient`).

Los corchetes son extras: `fastapi` a secas no trae uvicorn ni httpx;
`fastapi[standard]` sí. `uv tree` lo muestra con la marca `(extra: standard)`.
De 54 paquetes instalados, declaraste 3 — los otros 51 son prestados.
**Declara lo que importas**, aunque ya esté instalado por otra vía.

### Errores del día
1. `MODELS` redeclarado en `conftest.py` en vez de importado (identidad).
2. `def client(app)` — pytest solo llena parámetros de fixture con **otras
   fixtures**, buscando por nombre. `app` viene de `main.py`, no de pytest.
3. Fixture nombrada como verbo (`create_test_client`). Se nombran como
   sustantivo: **lo que el test recibe**.
4. Falta el comentario que justifica el `os.environ` encima de los imports.

---

## Parte 2 (sesión 13, 2026-08-11)

### Por qué existe respx
En la S9 verificaste el cliente robusto con tres experimentos manuales: levantar
`flaky_server.py`, apuntar a un puerto muerto, e inyectar un `ValueError`
editando el código. Funcionó una vez y no se repite solo.

El problema de fondo: tu cliente habla con **algo que no controlas**. Un test que
depende de un servidor ajeno prueba la red, no tu código.

`respx` **no es un micro-servidor** (esa fue tu hipótesis). No hay puerto, socket
ni proceso: sustituye el **transporte** de httpx, la capa que convierte tu
`Request` en bytes que salen por el cable. Tu código construye la request igual,
la `await`ea igual, `raise_for_status()` corre igual y el JSON se parsea igual —
pero nunca sale del proceso.

| | `flaky_server.py` (servidor real) | `respx` (transporte falso) |
|---|---|---|
| infraestructura | otra terminal, puerto, arrancar/apagar | ninguna |
| tiempo del timeout | 1.0s de verdad | 0s |
| simular "se cayó la red" | imposible: un servidor vivo tiene socket | `side_effect=` |
| saber qué te pidieron | no | `route.called`, `route.call_count` |

Se mockea la **frontera** (el servidor ajeno), **jamás tu propio código**. Eso
último es lo que hacía `unittest.mock.patch` sobre el método del cliente, y por
eso dejaba de probar justo lo que importa: tu manejo de errores.

### `return_value` vs `side_effect`
```python
respx.get(URL).mock(return_value=httpx.Response(500))        # el servidor contestó mal
respx.get(URL).mock(side_effect=httpx.ReadTimeout("slow"))   # NO hubo respuesta
```
Es la misma distinción que te costó dos iteraciones en la S9: excepción de red
vs error HTTP. Un 500 trae `Response`; un timeout no.

Request sin ruta registrada → respx lanza un error que significa *"se te olvidó
mockear esto"*. No es un 500: es un aviso para el autor del test.

### `pytest.raises` ES la aserción
Un test no falla "porque un assert dio False": falla **si escapa cualquier
excepción**. `assert` solo es azúcar que lanza `AssertionError`.

| dentro del `with` | veredicto |
|---|---|
| lanza lo declarado (o una hija) | pasa |
| no lanza nada | `Failed: DID NOT RAISE` |
| lanza otra cosa | falla con esa excepción |

Para afirmar **sobre** la excepción, `as exc_info` y el assert **fuera** del
bloque (dentro nunca se ejecutaría — el bug de S7).

### `parametrize`
```python
@pytest.mark.parametrize("value, expected", [(2, 4), (3, 9)])
def test_square(value, expected):
    assert value ** 2 == expected
```
Tres tests copiados que solo cambian en un número son **uno con tres datos**.
El string nombra; la lista trae una entrada por corrida (pelada si es un solo
parámetro); los nombres entran en la firma igual que una fixture.
`@respx.mock` va **arriba** del `parametrize`.

**No sirve para todo**: `parametrize` corre N veces *independientes*. El test del
escenario mixto necesita las 10 rutas registradas **a la vez**, porque
`fetch_all_robust()` dispara las 10 requests en una sola llamada. Ahí va un bucle
dentro de **un** test, no `parametrize`.

### Fixture async
```python
@pytest.fixture
async def client() -> AsyncIterator[httpx.AsyncClient]:
    async with httpx.AsyncClient(timeout=1.0) as http_client:
        yield http_client        # <- lo que el test recibe
```
- `async def` porque `async with` es una forma de `await`.
- Tipo `AsyncIterator[...]`, no el objeto: no lo devuelve, lo **produce**.
- **`yield` pelado entrega `None`**, igual que un `return` pelado (tu bug:
  `AttributeError: 'NoneType' object has no attribute 'get'`).
- Teardown implícito: al salir del `async with`, el cliente se cierra.
- El test la pide **declarándola como parámetro**; si no, `NameError`.
- `asyncio_mode = "auto"` en `pyproject.toml`: pytest solo no sabe correr una
  corrutina, necesita `pytest-asyncio` abriendo el event loop.

### ERROR vs FAILED
| dónde revienta | pytest reporta |
|---|---|
| fuera del cuerpo (colección, import, fixture) | **ERROR** — el test ni existió |
| dentro del cuerpo | **FAILED** — con la excepción que sea |

S12 (`KeyError` al importar) fue ERROR; la mutación de hoy fue FAILED causado
por `HTTPStatusError`. **Un test puede fallar sin que ningún assert se ejecute**
— esa fue la causa de fallar las dos predicciones.

### Cobertura
`uv run pytest --cov=robust_client --cov-report=term-missing` → **41%**,
faltando `34-61` y `65`: `summary()` y el `if __name__ == '__main__'`.

Coverage mide **líneas ejecutadas**, no calidad. Un número bajo puede significar
"código que no debería estar en el producto" (tu lectura, correcta: `summary` es
un helper de demo). Y el reverso importa más: **se puede tener 100% con cero
asserts** — tu `test_mixed_case` sin asserts pasaba en verde y no probaba nada.

### Auditoría de mutación (corrida por el estudiante)
| Rotura en `robust_client.py` | Resultado |
|---|---|
| sin `raise_for_status()` | 4 rojos (los 3 del parametrize + el mixto) |
| `return_exceptions=False` | `test_mixed_case` FAILED, sin ejecutar un solo assert |
| `response.text` en vez de `.json()` | rojos el happy path y el mixto |

Detalle fino: sin `raise_for_status()`, los del `parametrize` **no** caen por
"DID NOT RAISE" — un 401 con body vacío llega a `.json()` y revienta con
`JSONDecodeError`. Leer *por qué* se puso rojo importa tanto como que se puso.

### Errores del día
1. `yield` pelado en la fixture → el test recibió `None`.
2. Test sin parámetro `client` → `NameError` (fixture ≠ variable global).
3. `test_mixed_case` **sin un solo assert**, verde y probando nada.
4. `MODEL_IDS[1:9]` dejó los ids 0 y 9 sin mockear; `Response(200)` sin `json=`
   habría dado `JSONDecodeError`. Los dos invisibles detrás del verde de (3).
5. `parametrize` aplicado al escenario mixto (N corridas independientes cuando
   se necesitaban 10 mocks simultáneos).

---
Fuentes: documentación oficial de pytest (fixtures, conftest, parametrize),
FastAPI (`fastapi.testclient`), respx y pytest-asyncio. Verificado ejecutando en
el proyecto del estudiante.
