# Sesión 10 — 2026-08-05

## Qué se vio
`f1.apis-rest-fastapi` parte 1: por qué existe FastAPI (hints+Pydantic+async),
routing por decorador, path params tipados, body como modelo Pydantic,
HTTPException/404, 422 vs 400, status_code=201 en el decorador, /docs.
De paso se cobró la deuda de uv: proyecto real con `uv init --bare`,
`uv add "fastapi[standard]"`, lockfile y `uv run fastapi dev`.

(La sesión arrancó el 04 y cerró el 05 — el ejercicio se trabajó con calma.)

## Ejercicio y resultado
`ejercicios/fase-1/f1.apis-rest-fastapi/01-registro-modelos/` (completar +
predecir): Model Registry con 4 endpoints (health, listar, obtener con 404,
POST validado con 201). **Resuelto por su cuenta con pistas 1-2** (patrones
GET/POST con dominio distinto; la lógica fue suya). Verificado ejecutando:
200/404/201/422-body/422-path correctos. P2 (422 y quién decide) clavada.

Detalles:
- PRIMERA sesión sin sombrear builtins tras 3 avisos (`model_id`). En remisión.
- Aplicó `model_dump()` + `**` recién aprendidos en el POST; los GETs los
  refactorizó a `{"id": model_id, **MODELS[model_id]}` tras señalárselo.
- Pregunta al porqué (ids con `max()+1`): identificó solo el caso del store
  vacío (ValueError); el reciclaje de ids se le explicó. Candidata a review.
- P1 reescrita antes de ejecutar (análisis legítimo, aceptado) — se le dio la
  regla: conservar ambas versiones de una predicción.

## Estado
`f1.apis-rest-fastapi` → **aprendido**, next_review 2026-08-07, intentos 1.
Contenido del tópico INCOMPLETO: falta parte 2 (API key auth + mypy/pyright).
`posicion_actual` sigue en `f1.apis-rest-fastapi`.

## Próximo paso
La próxima sesión abre con 2 repasos VENCIDOS (2026-08-05): `f1.python-profesional`
(si pasa → dominado se mantiene, intervalo +21) y `f1.async-basico` (pregunta
candidata: por qué UN AsyncClient compartido; excepción no manejada en gather).
Luego FastAPI parte 2: API key auth (Header/Depends) + type-check formal.
Pregunta candidata review fastapi: los 2 fallos de `max(keys)+1`.
