---
tipo: sesion
fecha: 2026-09-01
sesiones: [20]
tags: [sesion]
---

# Sesión 20 — 2026-09-01 (arrancó la noche del 08-31)

**Tópicos:** [[APIs REST con FastAPI]] (ejercicio)

## Tópico
`f1.apis-rest-fastapi` — ejercicio `03-api-externa`, tests 4-5 y paso 7 (cierre del ejercicio).

## Qué se hizo
- **Fórmula "arrancar escribiendo" mantenida**: cero teoría previa, el estudiante entró directo al test 4 con su propia suite de S13 como referencia.
- **Test 4 (timeout → 504)**: primera versión con `with pytest.raises(httpx.TimeoutException)` alrededor del `client.get`. Pista 1 (¿dónde muere la excepción? ¿qué pregunta hace este test?) y lo corrigió ÉL: la excepción la atrapa el `try/except` del endpoint y al test llega una response 504.
- **Test 5 (moneda ausente → 502)**: primera versión verde POR LA RAZÓN EQUIVOCADA — **sexta aparición de la familia**. Sin `@respx.mock` la ruta se registra pero nadie la activa: la request fue al Frankfurter real (0.59s de call vs <0.005s del test 4). Se le entregó la evidencia de `--durations=3` y lo diagnosticó y arregló él. En la pregunta de cierre explicó lo esencial (el verde venía de la red real, no del mock); precisión dada: el 502 lo producía su guarda en ambos casos, lo que cambió es de dónde venían los datos.
- **Paso 7 verificado por el tutor ejecutando**: 12/12 verdes en 0.10s, `ruff check .` limpio, `mypy main.py conftest.py test_upstream.py` limpio.

## Resultado
- **Ejercicio `03-api-externa` COMPLETO** (S14→S20, siete sesiones — la deuda de rediseño ya está registrada). El entregable técnico del criterio de fase F1 existe y está verificado.
- `f1.apis-rest-fastapi` sigue en `aprendido`; su review venció hoy y no se corrió (el estudiante pidió cerrar — regla S19 aplicada, sin repaso con fatiga).
- Nit abierto no bloqueante: test 5 sin assert espía (`route.call_count == 1`).

## Próximo paso (S21)
1. **Abre con repasos, máximo 3** (todos vencidos): `f1.git-flujo` (2026-09-01), `f1.async-basico` (2026-09-01), `f1.apis-rest-fastapi` (2026-09-01). El de `f1.testing-pytest` vence 2026-09-02 — si hay hueco, es el experimento de invertir las 2 líneas del conftest corrido POR ÉL.
2. Para cerrar F1 quedan exactamente dos cosas: el 2º PR con conflicto de `f1.git-flujo` (cierra también su criterio de dominio) y la **variante de guardas sobre payload ajeno** (deuda S16). Cualquiera de las dos puede ser el bloque de producción de S21.
