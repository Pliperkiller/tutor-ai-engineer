---
tipo: sesion
fecha: 2026-09-15
duracion_min: 75
sesiones: [29]
tags: [sesion]
---

# Sesión 2026-09-15

> Sesión 29. Arrancó el 2026-09-14 por la noche y cruzó la medianoche.

**Tópicos:** [[Llamadas a APIs de LLM]] (visto → visto, ejercicio a medias)

**Ejercicio:** `ejercicios/fase-2/f2.llamadas-api/01-cliente-anthropic/` — `TODO(3) ask_with_retries` escrito y a medias. Lo que quedó y está verificado ejecutando: `try` con solo la llamada dentro y `return` inmediato, `except TRANSIENT_ERRORS as exc` con la tupla (el `401` del paso 7 escapó intacto, sin un solo reintento), la secuencia `2**N + jitter` medida en `1.21 / 2.63 / 4.81`, y el `print` anunciando la espera que viene. Lo que falta: re-lanzar el último error al agotar los intentos, la numeración de intentos (imprime `1,2,3` donde el contrato dice `0,1,2`) y la última espera, que es tiempo muerto. Pasos 7 (a medias) y 8 sin cerrar.

**Andamiaje nuevo del tutor:** `retry_check.py` — apunta el cliente a `127.0.0.1:9` para provocar un `APIConnectionError` real y ejercitar el bucle sin key, sin red y sin costo. Nació de un hueco del propio enunciado: ni el paso 5 ni el paso 7 hacían correr los reintentos.

**Repasos:** ninguno. El de [[Async y concurrencia básica]] vencía hoy, se dejó para el final y no hubo tiempo — se arrastra.

**Errores clave:** media corrección por segunda sesión seguida (al subir el cálculo de `sleep_time` arrastró el `retry_no += 1` y rompió lo que estaba bien); la cláusula del `raise` se señaló como lo grave y no se aplicó; el paso de tipo `predecir` se corrió sin escribir la predicción. Regresión de ritmo: ocho preguntas de sintaxis antes de escribir una línea, tres de ellas relecturas de lo escrito en el mensaje anterior — del lado del tutor, se rompió la regla de S17 de no pasar de dos re-explicaciones.

**A favor:** preguntó *dónde estaba el material* en lugar de afirmar que no se le había enseñado — la conducta opuesta a los reclamos infundados de S15 y S24. Y arregló solo, a la primera, dos de las tres cosas señaladas.

**Próximo paso:** S30 = aplicar los tres arreglos de `ask_with_retries` y volver a correr `retry_check.py` (esperado: intentos `0,1,2`, **dos** esperas y un traceback de `APIConnectionError` en vez de `RETURNED: None`), luego ruff, `RESULTADOS.md` y el paso 8 (`sampling_check.py` + tabla de la parte C). Sin lección nueva. Repasos vencidos acumulados: [[Async y concurrencia básica]] 09-14, [[Modelado y validación con Pydantic]] 09-15, [[Testing con pytest]] y [[APIs REST con FastAPI]] 09-16.
