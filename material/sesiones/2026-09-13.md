---
tipo: sesion
fecha: 2026-09-13
sesiones: [27, 28]
tags: [sesion]
---

# Sesión 27 — 2026-09-12 / 13 (Mac/zsh)

**Tópicos:** [[Llamadas a APIs de LLM]] (no_visto → visto)

Segunda sesión de F2 y primera que corre completa bajo el protocolo de dos sesiones: lección + preguntas, ejercicio no montado — cerró dentro del formato. Sin repasos al abrir (el de [[Cómo funciona un LLM]] venció a medianoche, DURANTE la sesión; ofrecido al cierre, el estudiante eligió moverlo — regla S19 — y abre la S28).

**Trabajo:** `leccion.md` + `demo_basic_call.py` + `demo_streaming.py` en `ejercicios/fase-2/f2.llamadas-api/`. Las 4 preguntas discutidas en el chat: Q1 mecanismo stateless→cuadrático bien (omitió el 10.º mensaje user del inventario); Q2 3/3 con conexión propia a "verde por la razón equivocada"; Q3 criterio de reintentos y paso a paso del 401 bien, con "429 es un timeout" corregido (distinción response vs excepción producida por él tras reformular); Q4 completa, con la frase clave suya sobre `usage` ("no se puede predecir la cantidad de tokens, toca generarlos").

**A favor:** antes de leer la lección aplicó stateless a esta misma conversación con el tutor ("¿recibes tooodo el chat cada vez?") y respondió bien la pregunta de control (sin archivos, el tutor no sabría nada de él). Propuso construir un mini-SDK con httpx contra su Ollama remoto (RTX 5080) — acordado como ejercicio extra del tópico, después del principal.

**Logística resuelta:** gestión de la API key (env var en el perfil del shell de cada máquina, nunca en el repo), qué es I/O (reanclado a su medición de S8).

**Próximo paso:** el estudiante crea su API key de Anthropic antes de la S28 (pasos al final de `leccion.md`). S28 = repaso vencido de [[Cómo funciona un LLM]] (ítems 1, 2 y 4 sobre todo; el 3 lo produjo hoy en frío) + sesión de EJERCICIO de [[Llamadas a APIs de LLM]] (demos + cliente con streaming, backoff a mano y costo; verificación de la parte C de `01-predecir-tokens` contra la API real). Repasos que caen: async 09-14, pydantic 09-15, testing-pytest y apis-rest-fastapi 09-16.

---

# Sesión 28 — 2026-09-13, noche (Mac/zsh)

**Tópicos:** [[Cómo funciona un LLM]] (visto, repaso superado) · [[Llamadas a APIs de LLM]] (visto → visto, ejercicio a medias)

Sesión de ejercicio, larguísima (20:09–23:30) y con dos incidentes que se comieron más de una hora. Aun así salió trabajo verificado.

**Repaso ([[Cómo funciona un LLM]]) — superado, intervalo +7.** ` gato` vs `gato` y `T=0` no determinista, de memoria y correctos. `top_p` falló en el primer intento con una **variante nueva** del error de la S26: ya no lo lee al revés, pero lo entendió como umbral *por token* ("los que tienen 10 % o más cada uno") en vez de acumulación desde arriba. Se autocorrigió al devolverle su propio caso de la S26 (tabla plana, top al 2 %). En el repaso del 09-20 hay que preguntarlo sin ese ejemplo.

**Ejercicio `01-cliente-anthropic/` — 2 de 3 TODO, verificados por el tutor ejecutando.** API key creada, demos corridas (incluida la de streaming, apareciendo a pedazos). `cost_usd` 3/3 y `ask` devolviendo tupla, también con `temperature=0.0`. Queda `TODO(3)` (backoff a mano) y los pasos 7-8.

**Lo mejor de la sesión — una pregunta suya corrigió el material (3.ª vez: S13, S26, S28).** Preguntó "¿cómo se asignan `temperature` y `top_p` a un cliente del SDK?". Además de la respuesta conceptual (no se asignan al cliente, que solo lleva credenciales y red, sino a la request), verificar la pregunta destapó que **esos parámetros ya no existen** en la Messages API de `anthropic` 1.5.0: el contrato que el tutor había escrito en el esqueleto pedía algo imposible. Llegan a Haiku 4.5 por `extra_body`, probado en vivo.

**Incidente de costo — culpa del tutor.** El tutor le indicó guardar la API key con `export` en `~/.zshrc`. Consecuencia no prevista: Claude Code hereda esa variable del entorno y factura contra los créditos de la API en vez de su suscripción. Lo detectó y lo resolvió **él**. Arreglo de raíz aplicado en sesión: la key vive en un `.env` del proyecto cargado con `uv run --env-file`, `.gitignore` ampliado a `.env.*`, y el paso 4 de `leccion.md` reescrito y fechado.

**Errores propios:** media corrección en `cost_usd` (tres pasadas: primero línea duplicada, luego cambió la tarifa pero no el conteo); `elif` dentro de una expresión ternaria; `out_text = block.text` sobrescribiendo en el bucle — aunque predijo sus dos consecuencias sin ayuda al preguntárselo.

**Próximo paso:** S29 = `TODO(3)` (backoff exponencial con jitter) + pasos 7 y 8 del enunciado, sin lección nueva. Ojo con los 2 `F401` de ruff (`random`, `time`): son los imports que ese TODO consumirá. Repasos vencidos acumulados: [[Async y concurrencia básica]] 09-14, [[Modelado y validación con Pydantic]] 09-15, [[Testing con pytest]] y [[APIs REST con FastAPI]] 09-16.
