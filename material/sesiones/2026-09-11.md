---
tipo: sesion
fecha: 2026-09-11
duracion_min: 75
sesiones: [26]
tags: [sesion]
---

# Sesión 26 — 2026-09-10 / 11 (Windows/PowerShell + nvim)

**Tópicos:** [[Cómo funciona un LLM]] (no_visto → visto)

Arrancó el 10 a las 11:15 y se cerró el 11: primera sesión de la Fase 2. Sin repasos vencidos (async cae 09-14). Sin API key en la máquina: demo y ejercicio con tiktoken local.

**Ejercicio:** `ejercicios/fase-2/f2.como-funciona-llm/01-predecir-tokens/` (tipo *predecir*): predicciones de tokenización (6 textos, 3 pares) verificadas con `count_tokens.py`, más 3 predicciones de sampling (T=0, T=1, T=1+top_p=0.1) que quedan ESCRITAS para verificarse contra la API real en `f2.llamadas-api`. Completo en `RESPUESTAS.md`, con las causas de los pares y el porqué de top_p corregidos en sesión. Andamiaje arreglado en sesión: `count_tokens.py` ahora imprime las piezas de los pares.

**Errores clave:** "gato es 1 token porque es simple" (simple ≠ frecuente); razonamiento correcto + casilla contradictoria en casa/` casa` (sin explicar, 3 preguntas); "fibonacci" por cuadrático; top_p leído al revés; parte B con datos y sin causas hasta la frase con huecos. Detalle en la nota del tópico.

**A favor:** rebatió con razón dos afirmaciones del tutor sobre sampling (la tabla real del prompt no se conoce; con top al 1-2 %, top_p=0.1 deja varios candidatos). Q3 (99/47, GPU) perfecta; Q2 (40 reenvíos, sin memoria) bien.

**Herramientas montadas (fuera del repo):** keymap `<leader>rc` en su Neovim (`lua/config/keymaps.lua`): normal = `uv run --project <carpeta del archivo> python <archivo>` en terminal partida; visual = selección a `uv run python`.

**Cambio de protocolo (`/config`, commit 6f56975):** el estudiante señaló que lección exhaustiva + preguntas + ejercicio no caben en 30 min. Desde ahora cada tópico nuevo son DOS sesiones: lección (leer + discutir preguntas) y ejercicio (repasos + ejercicio). Registrado en `state/agente.json`.

**Próximo paso:** S27 = sesión de LECCIÓN de [[Llamadas a APIs de LLM]] (estructura de mensajes, streaming, reintentos, tokens y costo; ahí entra la API key: pedirla y acotar gasto). Review de [[Cómo funciona un LLM]] el 09-13 (4 items en `progress.json`: espacio-pieza, top_p, cuadrático, T=0). Repasos que caen: async 09-14, pydantic 09-15, testing-pytest y apis-rest-fastapi 09-16.
