# Sesión 23 — 2026-09-09

**Máquina**: Windows/PowerShell (el estudiante alterna con la Mac; uv y el venv de 01-registro-modelos funcionan aquí).

## Repasos (los dos superados)

### f1.testing-pytest — SUPERADO, sube a `aprendido` (next_review 2026-09-16)
- El experimento del conftest montado en S22 lo corrió **él**: predicción escrita antes ("corren 3 de 5, ERROR con KeyError, revienta en conftest mientras corre un test"), líneas invertidas, `Remove-Item Env:` + `uv run pytest -v`, lectura del output literal.
- Se autocorrigió con la evidencia delante: "no corre ninguno, se rompe antes de correr cualquier test". Produjo él las dos piezas que faltaban desde S12/S13/S19: el KeyError nace en `main.py:42` (conftest solo dispara el import) y pytest se rompió **en la colección** (ERROR, 0 tests).
- R2b también suya: los asserts dentro del `with pytest.raises` nunca corren; reescritura correcta a la primera (solo la línea que explota dentro, `exc_info` fuera). Las 3 salidas de memoria, `DID NOT RAISE` incluido.
- Conftest restaurado y verificado: 5 passed.

### f1.apis-rest-fastapi — SUPERADO, sube a `aprendido` (next_review 2026-09-16)
- La cadena DI de memoria, sin abrir archivos: lifespan / app.state / get_client / Depends / Annotated, cada pieza con su papel. Precisión dada: Annotated = tipo + **metadata** (no "valor").
- Pregunta de control bien: 50 requests → **un** AsyncClient con pool de **conexiones** dentro. El "pool de clientes" de S22 corregido.
- Matiz: la primera respuesta fue una cadena de flechas con el orden torcido, y esquivó la pregunta de control 3 mensajes. Próxima review: la cadena sin darle la lista de piezas.

## Ejercicio
- **Montado, sin empezar**: `ejercicios/fase-1/f1.git-flujo/03-pr-conflicto/` (enunciado + RESPUESTAS.md) — el que cierra el criterio de dominio de git-flujo: 2 PRs sobre la misma línea del README de asistente-config, el B queda en conflicto y se resuelve vía PR.
- El estudiante pidió cerrar antes de arrancarlo (regla S19: se cierra). La sesión cerró en orden y con producción: los dos repasos fueron con ejecución real.

## Próximo paso (S24)
1. Ejercicio 03-pr-conflicto desde el paso 0 (clonar asistente-config fuera del repo tutor, en la máquina del día). Evaluar sin recordárselo: `git status`/`branch` y `switch -c`.
2. Repasos que caen: async 09-14 (las DOS mitades solo → +21), pydantic 09-15, testing-pytest y apis-rest-fastapi 09-16.
3. Pendiente de fase: variante del ejercicio de guardas sobre payload ajeno.

---

# Sesión 24 — 2026-09-09 (tarde, Mac/zsh)

Sin repasos vencidos (el próximo cae 09-14). Sesión 100% de producción: el ejercicio montado en la mañana.

## Ejercicio 03-pr-conflicto — COMPLETO. Criterio de dominio de git-flujo CERRADO → **dominado**

- Verificado ejecutando: 3 PRs fusionados en `asistente-config` (#2 y #3 hoy), 14 commits, conflicto real en README resuelto vía PR (merge local `61d8a3d` + PR #3), cero marcadores, línea final mezcla ambos lados con dedup correcto.
- Las 3 predicciones escritas y al final correctas. P1 necesitó la pregunta discriminante ("¿y si A tocara OTRA línea?") para pasar de "main adelantado" a "misma línea tocada". P3 impecable a la primera: 5 commits nuevos, 3 merges con su origen.
- Pistas nivel 1-2 (sintaxis `push -u` y `gh pr create` como tooling). `--no-rebase` fue hueco del ENUNCIADO (Mac fresca sin `pull.rebase`) — corregido en el enunciado, concedido explícitamente.
- Reclamo infundado #2: "nunca me enseñaste los marcadores" — verificado falso (material línea 40, conflictos S4 y S5). Se le mostró el registro; regla acordada: releer `material/` antes de reclamar.
- Nits nuevos: prefijo `Carlos C :` en commits, títulos de PR "merge X". Van a la review del 09-24.
- Aprendizajes nuevos en sesión: `-u`/upstream, merge vs rebase como estrategias de pull divergente (rebase solo presentado), estado `(MERGING)`.

## Próximo paso (S25)
1. Repasos que caigan: async 09-14 (las DOS mitades sin empujón → +21), pydantic 09-15, testing-pytest y apis-rest-fastapi 09-16 (cadena DI sin darle la lista).
2. Producción: variante PEQUEÑA del ejercicio de guardas sobre payload ajeno — lo único que queda para cerrar F1.
3. Con eso, F2 arranca en `f2.como-funciona-llm` (posicion_actual ya apunta ahí).

---

# Sesión 25 — 2026-09-09 (16:44, Mac/zsh) — (pausada)

- Sin repasos vencidos. Ejercicio de la variante de guardas MONTADO en `ejercicios/fase-1/f1.apis-rest-fastapi/04-guardas-payload/` (Open-Meteo, 2 guardas, 5 tests dados, trampa de `temperature` en dos niveles).
- El estudiante preguntó qué eran las guardas → antídoto S24 aplicado: remitido a `material/fase-1/f1.apis-rest-fastapi.md` (~191 y ~252) con recordatorio mínimo. Pausó ahí con `/break`, sin empezar a producir (paso 0 de 6).
- Deuda de tooling saldada: `setup.sh` de 01-branches-conflicto ya pide confirmación antes del `rm -rf`.
- Sin cambios de estado en `progress.json`. Contexto completo en `state/sesion_en_curso.md`.
- Al retomar: paso a) del enunciado (uv init + deps), P1 antes del primer pytest, las 2 guardas. Completarlo cierra F1.
