---
tipo: sesion
fecha: 2026-09-16
duracion_min: 90
sesiones: [30]
tags: [sesion]
---

# Sesión 2026-09-16

**Tópicos:** [[Llamadas a APIs de LLM]] (visto → aprendido), [[Cómo funciona un LLM]] (visto → aprendido)

**Ejercicio:** `ejercicios/fase-2/f2.llamadas-api/01-cliente-anthropic/` — **COMPLETO y verificado ejecutando**, tras tres sesiones (S28, S29, S30). Se cerraron los tres cabos de `TODO(3) ask_with_retries`: re-raise del error original, numeración de intentos y eliminación de la última espera muerta. Más el paso 7 (`.env.broken` → `AuthenticationError` 401, cero reintentos) y el paso 8 (`sampling_check.py`, 15 requests), con `RESULTADOS.md` lleno y `ruff check .` limpio.

**Lo mejor del día:** la verificación del paso 8 cerró de golpe el error conceptual de `top_p` que venía abierto desde la S26. Predijo 5 respuestas distintas para `top_p = 0.1`, la API devolvió 1, y **escribió la causa correcta por su cuenta** (*"'PlantAlert' ya cubre un 10% de probabilidad"*). Eso, más la pregunta de control de `top_p = 0.9` respondida sin andamiaje, es lo que sube [[Cómo funciona un LLM]] a `aprendido`.

**Errores clave:** "arregla la mitad del arreglo" apareció **dos veces** (movió el `raise` y arrastró el `print` debajo del `sleep`; borró la línea 131 pero no la 132 → `NameError` antes de entrar al bucle) — tercera y cuarta aparición en tres sesiones. Y dijo "listos" sin correr el comando de verificación que se le había dado literal en la misma instrucción.

**Del lado del tutor — dos fallos, uno grave:**
1. **Key expuesta.** Para diagnosticar de dónde salía `ANTHROPIC_API_KEY`, el tutor corrió `echo ${ANTHROPIC_API_KEY:-NO}`, que **imprime el valor**. La key quedó pegada en el chat. Es el mismo cabo que la S28 dejó abierto. **Pendiente de confirmar en la S31: rotar la key** (revocar en `console.anthropic.com`, poner la nueva en `.env`). La única forma admitida de comprobar que un secreto existe es `echo "${VAR:+ESTA PUESTA}"`.
2. **Diagnóstico falso, refutado por el estudiante.** El shell de la herramienta Bash del tutor tiene la key en el entorno; el tutor corrió el paso 7 desde ahí, le salió el camino feliz, y concluyó que el experimento del estudiante era inválido y que él había exportado la variable a mano. El estudiante respondió con la corrida de **su** terminal (401 correcto) y tenía razón en todo. Cuarta vez que corrige al tutor con fundamento (S13, S26, S28, S30).

**Hallazgo técnico que sí sirve** (lo único bueno del enredo): **`uv run --env-file X` no sobrescribe una variable que ya exista en el entorno** — el shell gana, el archivo solo rellena huecos. Comprobado en aislado con `PROBE_VAR=from_shell uv run --env-file probe.env ...` → `from_shell`. El paso 7 del enunciado solo funciona en una terminal donde la variable no esté exportada; hay que anotarlo ahí.

**Configuración cambiada** (`/config-tutor`, commit `a9b3a19`): a mitad de sesión el estudiante pidió *"dime un paso a la vez en vez de escupirme todo lo que falta"*, funcionó el resto de la sesión, y al cerrar lo fijó como regla permanente — corrigiendo además la redacción del tutor (*"un paso no tiene que ser una sola cosa"*). Quedó como punto 8 de `CLAUDE.md`: un paso es **un bloque que se verifica junto**.

**Próximo paso:** **sesión de `/repaso` dedicada** antes de abrir tópico nuevo. Hay cuatro vencidos de F1 y dos sesiones seguidas sin correrlos: [[Async y concurrencia básica]] (09-14), [[Modelado y validación con Pydantic]] (09-15), [[Testing con pytest]] (09-16) y [[APIs REST con FastAPI]] (09-16). Después, [[Prompt engineering estructurado]] como tópico nuevo (`posicion_actual`), en su sesión de lección.
