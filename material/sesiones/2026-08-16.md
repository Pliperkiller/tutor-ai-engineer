# Sesión 15 — 2026-08-16

**Tópico:** f1.apis-rest-fastapi (parte 3, cierre de Fase 1) — continuación de la S14
**Duración:** muy por encima de los ~30 min del protocolo (otra vez)

## Repasos (2 vencidos, ambos fallados el 13/08)

### f1.async-basico — SUPERADO
Pregunta: reanclada a su propia medición de la S8 (10 requests de 0.3s → 3.14s secuencial
vs 0.43s con `gather`), y luego la de la S14 que había fallado (10 requests de 0.8s con
`AsyncClient(timeout=1.0)`).

Respondió **~0.8-1s** — correcto. La regresión de la S14 (había dicho "~8 segundos",
sumando secuencialmente) queda corregida. Trajo además el matiz correcto de que
`timeout=1.0` es **por request**, así que ninguna falla.

Precisión dada: su explicación del *porqué* se apoyaba en el handshake/pool. El motor real
es que `await` sobre I/O devuelve el control al event loop y las diez esperas **se solapan**;
el total ≈ **la más lenta**, no la suma. El pool es ahorro de segundo orden.
Regla acordada: `gather` → **max**, no **sum**.

Vuelve a `aprendido`, next_review 2026-08-23 (+7).

### f1.testing-pytest — SUPERADO
Deuda explícita de la S13: escribir de memoria la fixture async del cliente httpx.
La escribió **exacta**: decorador, `-> AsyncIterator[httpx.AsyncClient]`, `async with`,
`yield http_client`.

Justificación del `yield` correcta en el mecanismo genérico (teardown, la función no
termina en esa línea). Precisión dada sobre la consecuencia concreta, que era el gap de
la S14: con `return http_client` el `async with` sale de inmediato → `aclose()` → el test
recibe un **cliente ya cerrado** y revienta en el primer `await`. Corregido también un
detalle suyo: `return http_client` sí devuelve el cliente; `None` lo devolvería un
`return` pelado.

Vuelve a `aprendido`, next_review 2026-08-23 (+7).

## Ejercicio — `03-api-externa`, pasos 4-8

Se retomó donde quedó la S14 (paso 3 de 8).

**Deuda de la S14 cerrada:** corrió `ruff` y arregló los 2 avisos por su cuenta
(`from asyncio import timeout` fuera, `AsyncIterator` desde `collections.abc`).
`lifespan=lifespan` ya estaba puesto desde el cierre de la S14.

**Paso 4 (endpoint `GET /models/{model_id}/cost`): a medias, camino feliz funcionando.**

Logrado y verificado ejecutando contra la API real (Frankfurter, con red):
- `?currency=EUR` → 200 `{"id":1,"name":"claude-fable-5","currency":"EUR","rate":0.86453,"cost":165.99}`
- `?currency=eur` → 200, mismo resultado (tras 3 iteraciones)
- `/models/999/cost` → 404
- Aritmética verificada: 64000/1000 × 3.0 = 192 USD × 0.86453 = 165.99
- `mypy main.py` limpio
- 404 decidido **antes** de tocar la red; `try/except` con 504/502 diferenciados;
  `CostOut` declarado; función renombrada `quote` → `get_cost`

Pendiente al cierre (3 puntos del contrato + ruff):
1. Sin variable para la moneda normalizada: `currency.upper()` escrito 4 veces.
2. La guarda de `rates` sigue siendo **código muerto** (está debajo de la línea que ya
   hace `payload["rates"]`). Punto 4.3 del contrato sin cumplir: un 200 sin `rates`
   da 500, no 502.
3. Firma `-> dict` + `return cost_out.model_dump()` en vez de `-> CostOut` + `return cost_out`.
   Punto 4.5 sin cumplir.
4. `ruff check .` → **6 errores**: F401 `from pyexpat import model` (autoimport del editor),
   F811, B008 (falta el `# noqa: B008` del `Depends` en default), SIM118 ×3 (`.keys()`).

**Pasos 5-8 sin empezar:** P1, la fixture del `TestClient` como context manager,
los 5 tests con respx, y el paso 7 de verificación.

### El momento didáctico del día
La secuencia de tres 500 consecutivos con `?currency=eur` fue el mejor material de la
sesión, y salió de que **él** corriera el servidor y los curls:
- 502 falso (guarda con `.upper()`, lookups sin él)
- `KeyError` en `rate=`
- `KeyError` en `cost=`

Cada arreglo parcial destapaba el siguiente sitio olvidado. Es la demostración empírica de
por qué un valor usado 4-5 veces se nombra una vez en una variable.

### Guarda invertida — REAPARICIÓN
Escribió `if not "rates" not in payload.keys():` — doble negación que dispara el 502
exactamente cuando el payload **está bien**. Se detectó porque el curl del camino feliz
devolvió `{"detail":"rates not in upstream"}` sobre una respuesta válida de Frankfurter.
Esta debilidad se había **retirado en la S11** tras dos sesiones limpias. Vuelve a vigilancia.
Antídoto acordado: leer la condición en voz alta como frase.

### Ritmo — MEJORA REAL
Primera sesión en cuatro donde el patrón se rompe. Hizo 4 preguntas, pero:
- 2 fueron de lectura del enunciado (respondidas en una línea),
- 1 fue de ingeniería genuina y buena (*"definir el modelo en `main` ¿no es mala práctica?"* —
  se le dio la estructura estándar de FastAPI y el umbral real para partir el archivo),
- y sobre todo: **levantó el servidor y iteró con curls por su cuenta**, sin pedir permiso
  ni validación entre intento e intento. El bucle de feedback dejó de pasar por el tutor.

Contrapunto: reclamó *"¿y de dónde carajos la iba a sacar?"* por la URL de Frankfurter,
que sí estaba en el enunciado dos veces (Contexto línea 20 y paso 4.3). Se le mostró.
Lo que en efecto **no** estaba era el nombre `FRANKFURTER_URL` ni la decisión de hacerla
constante de módulo — eso se lo regaló el tutor y se reconoció.

## Estados actualizados
- `f1.async-basico`: visto → **aprendido**, next_review 2026-08-23
- `f1.testing-pytest`: visto → **aprendido**, next_review 2026-08-23
- `f1.apis-rest-fastapi`: sigue **aprendido** (ejercicio de fase a medias), next_review 2026-09-01
- `posicion_actual`: sin cambio — `f1.apis-rest-fastapi`

## Próximo paso (S16)
Arrancar directo en el ejercicio, sin tópico nuevo:
1. Los 3 puntos pendientes del endpoint + los 6 de ruff (~5 min).
2. Paso 5: P1 escrita **antes** de tocar nada, y ajustar la fixture `client`.
3. Paso 6: los 5 tests con respx.
4. Paso 7: `pytest -v` (10-12 verdes, incluidos los 5 viejos), `ruff`, `mypy`.

Con eso queda cumplido el `criterio_dominio_fase` de F1 salvo `f1.git-flujo`
(falta el 2º PR con conflicto resuelto vía PR).
