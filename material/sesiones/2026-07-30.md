# Sesión 7 — 2026-07-30

## Qué se vio
`f1.pydantic-validacion` parte 2: modelos anidados (Forma A objeto vs
Forma B dict crudo y su efecto en `loc`), `model_validator(mode="after")`
para reglas multi-campo, `Field(default_factory=...)`, serialización y
roundtrip (`model_dump_json` / `model_validate_json`). De contrabando:
mecánica básica de pytest (`assert`, `pytest.raises`, `match`, asserts
muertos dentro del with).

## Apertura
- Recuperación activa de la pregunta abierta de S6 (cómo acumula Pydantic
  errores): hipótesis parcial ("holdea la excepción hasta instanciar") →
  corregida a atrapar-y-acumular por campo. Concepto asentado.
- Sin repasos vencidos (git-flujo vence 31/07, python-profesional 05/08).

## Ejercicio y resultado
`ejercicios/fase-1/f1.pydantic-validacion/02-asistente-anidado/` (tipo
script + predicción): 3 modelos anidados con validators y `test_model.py`
propio (esqueleto con 2 ejemplos dado por el tutor al detectar que pytest
como tópico no se había enseñado; los 6 casos restantes escritos por el
estudiante). **8/8 en verde, verificado ejecutando pytest.** P1 (loc de
error anidado) predicha correctamente: `("modelo", "temperatura")`.

Errores cometidos y corregidos por el estudiante (pistas nivel 1-2):
`self.name` vs `self.nombre`, asserts muertos dentro del `with
pytest.raises`, test 5 construyendo el anidado con Forma A (no ejercitaba
la anidación). El patrón de guard invertido NO reapareció (3/3 derechas).

## Estado
- `f1.pydantic-validacion`: visto → **aprendido** (criterio de dominio del
  tópico cumplido: 3+ modelos anidados + validators + tests propios).
  `next_review` 2026-08-01.
- `posicion_actual` → `f1.async-basico`.

## Próximo paso
- Repasos que vencen: `f1.git-flujo` (31/07) — incluye cerrar su criterio
  de dominio (2º PR con conflicto resuelto vía PR); `f1.pydantic-validacion`
  (01/08).
- Deuda de recuperación activa: ¿qué excepción lanza `self.name` cuando el
  campo se llama `nombre`? (AttributeError — cobrar sin material a la vista).
- Tópico nuevo siguiente: `f1.async-basico` (event loop, async/await, httpx).

---

# Sesión 8 — 2026-07-30 (segunda del día)

## Qué se vio
`f1.async-basico` parte 1: por qué asyncio (I/O-bound, threads antes),
concurrencia vs paralelismo, corrutinas, event loop, `await` como "me pauso
y libero al loop", `gather` vs await secuencial, `httpx.AsyncClient` con
`async with` y `raise_for_status()`.

## Apertura
Deuda de S7 cobrada, ambas correctas: AttributeError (no ValidationError,
"name no es un campo de esa clase") y model_dump→dict vs model_dump_json→str
con criterio de uso bien puesto. Sin repasos vencidos.

## Concepto
Sesión muy socrática: el estudiante pidió definiciones (concurrencia,
corrutina, gather, run, event loop) antes de predecir. Confusión importante
detectada y corregida: creía que `await` = "sigo con otra cosa"; se asentó
que await PAUSA al que lo escribe y la concurrencia la crea REGISTRAR varias
tareas antes de esperar. P3 (for+await vs gather) predicha 3s/1s con el
porqué correcto; verificado en vivo (3.02s/1.00s).

## Ejercicio y resultado
`ejercicios/fase-1/f1.async-basico/01-cliente-concurrente/` (tipo
completar+predecir): server.py mock del tutor (0.3s/request), client.py con
3 TODOs. Resuelto por su cuenta (solo sintaxis httpx como ayuda).
**Verificado ejecutando: sequential 3.14s, concurrent 0.43s (7.3x),
results match OK.** Predicciones escritas y correctas. Detalle fino que
cerró solo: el `gather` quedó DENTRO del `async with` (cliente vivo durante
las requests). Smell: variables `iter` e `id` sombrean builtins (avisado).

## Estado
- `f1.async-basico`: **aprendido** (criterio de dominio del tópico cumplido).
  `next_review` 2026-08-01. `posicion_actual` sigue en `f1.async-basico`:
  falta la parte 2 (timeouts, errores en gather) antes de dejarlo.

## Próximo paso
- Repasos: `f1.git-flujo` vence 31/07 (cerrar criterio: 2º PR con conflicto
  vía PR); `f1.pydantic-validacion` y `f1.async-basico` vencen 01/08.
- Pregunta abierta para cobrar: ¿por qué la concurrente dio 0.43s y no
  0.30s? (overhead no-red: conexiones TCP, scheduling, threads del server).
- Parte 2 de async: timeouts + `return_exceptions` en gather.
