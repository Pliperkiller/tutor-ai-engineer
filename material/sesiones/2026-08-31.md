---
tipo: sesion
fecha: 2026-08-31
sesiones: [19]
tags: [sesion]
---

# Sesión 19 — 2026-08-31

**Tópicos:** [[APIs REST con FastAPI]] (ejercicio), [[Testing con pytest]] (ejercicio + repaso fallado)

## Qué se vio
- `f1.apis-rest-fastapi` · ejercicio `03-api-externa`, paso 6: tests 2 y 3 de
  `test_upstream.py` + P2. Sin teoría nueva — la sesión ARRANCÓ escribiendo,
  según lo acordado en S18, y funcionó: hubo producción desde el minuto 1.
- Repaso de `f1.testing-pytest` al FINAL (orden invertido a propósito).

## Ejercicio y resultado
- **P2** respondida bien: 502 y no 404 porque la culpa es del upstream; el
  matiz (repetir el 404 mentiría sobre QUÉ no existe) lo aplicó vía assert.
- **Test 2** (`404 sin tocar la red`): primera versión VERDE POR LA RAZÓN
  EQUIVOCADA — `client.get("GET /models/999/...")` con el verbo dentro del
  string: el 404 que asserteaba era del router, no de su guarda. 5.ª aparición
  de la familia (S7, S12, S13×2). Detectado por el tutor con pregunta
  socrática; arreglado por él con la URL correcta + `assert` sobre el
  `detail` exacto ("Model not found") — más fuerte que lo pedido.
- **Test 3** (`502 con parametrize`): bien montado a la primera
  (`@respx.mock` + `@pytest.mark.parametrize`). Nit: usó 401/403/500 en vez
  de los 404/500/503 del enunciado; corregido al señalarlo.
- **Verificado ejecutando**: 12/12 verdes (los 2 nuevos portantes), ruff
  limpio SIN pedírselo (hábito de S14-S15 cobrado).
- Tests 4 y 5 siguen con cuerpo `# TODO` (su PASSED miente — él ya sabe por
  qué). Quedan para la S20 por decisión suya de cortar a tiempo: bien cortado.

## Repaso f1.testing-pytest (vencido del 2026-08-28) — PARCIAL
- R1: el mecanismo grande SÍ, de memoria — "para jalar app hay que tener la
  env var definida antes; el import ejecuta main y estalla ahí". El gap
  'from X import Y ejecuta X entero' + timing-no-scope está cerrado.
- R1, pieza "dónde revienta": no producida de memoria — dijo "FAILED, fallan
  todos los tests (TestClient falla)"; la realidad es ERROR en colección,
  0 tests. Se autocorrigió tras pista que telegrafiaba la respuesta.
  ATENUANTE REAL: fatiga declarada, y el repaso se corrió DESPUÉS de que él
  pidiera cerrar — señal contaminada, no concluyente. Se repregunta en frío.
- LECCIÓN DEL TUTOR (el estudiante tenía razón al reclamarlo): cuando el
  estudiante pide cerrar, se cierra — el repaso vencido se mueve a la
  siguiente sesión. Regla anotada en progress.json.
- R2: identificó la tensión (espera excepción vs llega response) pero las dos
  piezas (DID NOT RAISE; assert dentro del with jamás corre) las dio el tutor
  por cierre. NO producidas.
- Resultado: sigue `visto`, next_review +2 → 2026-09-02.

## Estado
- `f1.apis-rest-fastapi`: sigue `aprendido` (el ejercicio avanza; criterio de
  fase aún abierto hasta tests 4-5 + variante de guardas).
- `f1.testing-pytest`: sigue `visto`, next_review 2026-09-02.

## Próximo paso (S20)
1. ARRANCAR ESCRIBIENDO (la fórmula funcionó): tests 4 (timeout → 504, con
   `side_effect` — su propia suite S13 tiene el patrón) y 5 (200 sin la
   moneda en `rates` → 502; pista dada: pedir una moneda que UPSTREAM_OK no
   trae, p. ej. JPY). Con eso el paso 6 queda completo → paso 7 (verificación
   final: pytest + ruff + mypy).
2. Repaso testing-pytest DESPUÉS, sin escena: (a) invertir las 2 líneas del
   conftest → ¿ERROR o FAILED, cuántos tests corren? — idealmente que corra
   el experimento él mismo; (b) R2 completa de memoria.
3. Pendientes de fase sin cambio: variante de guardas sobre payload ajeno,
   2.º PR con conflicto en f1.git-flujo.
