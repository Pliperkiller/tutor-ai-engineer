---
tipo: sesion
fecha: 2026-08-27
sesiones: [18]
tags: [sesion]
---

# Sesión 18 — 2026-08-27

**Tópicos:** [[APIs REST con FastAPI]] (enseñado + ejercicio), [[Testing con pytest]] (ejercicio montado)

**Tópico:** `f1.apis-rest-fastapi` parte 3 (cierre de Fase 1) — pasos 5 y 6
**Cerrada a petición del estudiante:** *"por andar con tantos rodeos ya no tengo tiempo de seguir"*.

## Lo que dominó la sesión: feedback del estudiante sobre cómo enseño

Dos intervenciones, ambas con razón, ambas guardadas como reglas permanentes.

### 1. "Los conceptos se me quedan muy en el aire"

> *"aún no tengo claro por qué uno crea un `lifespan`, por qué luego hace un `get_client`, cuál es
> la necesidad del `Annotated[httpx.AsyncClient, Depends(get_client)]` y de inyectar dependencias.
> Quiero que seas conciso pero no escatimes en explicar las cosas y los conceptos más que en el
> mismo código."*

Diagnóstico suyo, y es correcto: escribe código que funciona sin el modelo mental de por qué la
pieza existe. Varias debilidades registradas (timing vs scope, guardas, "dónde revienta el código")
son eso — mecanismo ausente, no sintaxis ausente.

Se explicó la cadena completa (apuntes en `material/fase-1/f1.apis-rest-fastapi.md`): cliente
compartido → `lifespan` → `app.state` → `Depends` → `Annotated`, cada pieza como respuesta al
problema que deja abierta la anterior. **Contestó bien la pregunta de control**: por qué `aclose()`
va debajo del `yield` — *"si lo ponemos antes, abrimos, cerramos, y luego lo entregamos para
servir"*. Correcto y con sus palabras.

### 2. "Estás planificando muy mal las sesiones y los ejercicios"

> *"me pides que haga una cosa de 'una línea' donde en realidad hay muchas cosas a contemplar. Si
> no estás seguro si domino esto, primero necesito andamiaje y paulatinamente irme soltando, no
> mandarme al abismo. Con esto conviertes sesiones de media hora en cosas que llevo días haciendo."*

**Concedido con evidencia:** `03-api-externa` lleva las sesiones **14, 15, 16, 17 y 18**. Reúne diez
piezas nuevas en un entregable — `lifespan`, `asynccontextmanager`, `app.state`, DI, `Annotated`,
cliente httpx compartido, guardas sobre payload ajeno, `respx`, `parametrize`, fixtures generadoras
— y ninguna venía marcada como andamiaje frente a evaluable. Error de diseño del tutor.

**Reglas nuevas y permanentes:** (1) una pieza nueva por ejercicio; (2) el andamiaje va MARCADO en
el enunciado; (3) verificar en `material/` si algo se enseñó de verdad antes de pedirlo — si no, se
da con el concepto; (4) un ejercicio que cruza a una 2ª sesión se parte; a una 3ª, se rediseña.

**Matiz verificado antes de conceder** (regla de la S15): `AsyncIterator[...]` sí estaba en su
`main.py:40`, en `demo_upstream.py:28`, en el conftest del ejercicio anterior y en sus apuntes; y en
el mensaje se le dio el tipo exacto y el import. Lo que faltaba de verdad — y es lo que pesa — es
que **nadie le explicó nunca por qué**, y la versión síncrona (`Iterator`) no había aparecido jamás.

## Trabajo técnico

### Hecho por el estudiante
- **P1 confirmada correcta**: acertó la línea (`request.app.state.http_client`) y `FAILED`. Le
  faltaba el nombre de la excepción.
- Borró el cuerpo viejo de `test_cost_happy_path` (paso c).
- Primer intento de la fixture: `with TestClient(app) as client:` correcto — **falló el `yield` a
  secas**, que entrega `None`.

### Error del día: leer un traceback (nuevo, dos veces)
Respondió **"NoneType"** como nombre de excepción, dos veces. `NoneType` es un *tipo*, no una
excepción; aparece dentro del **mensaje**. Regla dada: **el nombre va antes de los dos puntos**.
Se zanjó ejecutando su propio código sin lifespan: `hasattr` → `False`, `AttributeError: 'State'
object has no attribute 'http_client'`. Nada se crea y nada vale `None`: el atributo no existe.

### Dado por el tutor (andamiaje, NO evaluado)
- `conftest.py`: fixture `client` con `Iterator[TestClient]` + `with` + `yield test_client`, y
  `reset_models_store` anotada. Se dieron las 3 pistas antes de resolver.
- `test_upstream.py`: **test 1 (camino feliz) como demo trabajada de respx**, marcada en el archivo.
  Se le había saltado el paso "demo mínima" de la secuencia de enseñanza y se fue directo a
  "escribe cinco".
- Verificado por el tutor antes de entregarlo: **10 verdes**, la fixture no rompió `test_main.py`.

## Estados

- `f1.apis-rest-fastapi`: sigue **aprendido** (tests 2-5 sin empezar; fixture y test 1 fueron dados).
- `f1.testing-pytest`: sigue **visto**, next_review 2026-08-28. **El repaso no se corrió.**
- `posicion_actual`: sin cambio.

## Próximo paso (S19) — arranca ESCRIBIENDO, cero teoría previa

1. **Test 2** `test_unknown_model_never_calls_upstream`: `/models/999/cost?currency=EUR` → 404 y el
   assert que importa es sobre `route` (no se llamó al upstream).
2. **P2** (línea 16) y **test 3**: `httpx.Response(404)` → 502, luego `@pytest.mark.parametrize`
   con 404/500/503.
3. Si sobra: tests 4 (`side_effect` de timeout → 504) y 5 (200 sin la moneda en `rates` → 502).
4. Verificación: `uv run pytest -v`, `ruff`, `mypy`.
5. El repaso de `f1.testing-pytest` (vencido el 28) va **después**, no antes.

Nota: el F401 de `pytest` en `test_upstream.py` es esperado — se resuelve al escribir el
`parametrize`.
