# Sesión 2026-07-27 — Diagnóstico (Sesión 0)

## Qué se hizo
- **Setup de infraestructura**: la carpeta no era un repo git. Se inicializó git local (`git init -b main`) + primer commit. Falta conectar remoto GitHub (sin `gh` CLI instalado; pendiente URL del estudiante).
- **Diagnóstico ejecutado** (protocolo `/diagnostico`).

## Autoevaluación por fase
- F1: Parcial → rebajado. F2–F8: No.

## Verificación
- El estudiante pidió **"empezar desde cero"** antes de la verificación conceptual/código.
- No se corrió ejercicio de código: sin afirmación que verificar hacia arriba, el estado conservador es todo `no_visto`.
- Calibración de ritmo: **"programo algo, Python básico"** — funciones/loops básicos, sin type hints, sin entornos, sin estructura de paquetes.

## Resultado
- `diagnostico.estado = completado`.
- `posicion_actual = f1.python-profesional`.
- Todos los tópicos en `no_visto`.

## Próximo paso
- Primera sesión real con `/sesion`: **f1.python-profesional** (type hints, dataclasses, manejo de excepciones, estructura de paquetes, entornos con uv).
- Verificar en vivo la base de Python autodeclarada (funciones/loops) al arrancar.

## Pendientes de infraestructura
- [ ] Crear repo vacío en GitHub y pasar URL → `git remote add` + `push`.
- [ ] Decidir identidad git del repo (commits actuales firmados como `vanesavega <vegavanesa786@gmail.com>`, correo del estudiante es `pip.kro@gmail.com`).
- [x] `uv` verificado: **NO instalado** (tampoco `ruff`). `pytest` sí. Se pospone uv/ruff a una sesión posterior del tópico.

---

# Sesión 1 — 2026-07-27

- **Tópico:** f1.python-profesional (F1)
- **Duración:** ~30 min
- **Estado final del tópico:** `visto` (el criterio_dominio del tópico exige aún uv/ruff/type-check/dataclasses/paquete).

## Qué se vio
Núcleo de "Python profesional": type hints en firmas, excepciones propias,
control de flujo (`continue`), métodos de string (`split`, `strip`,
`split(sep, 1)`) y diferencia listas vs. dict (asignación por clave).

## Ejercicio
`ejercicios/fase-1/f1.python-profesional/01-tags-tipados/` — tipo `completar`.
Implementar `parse_tags(raw: str) -> dict[str, str]` con normalización,
salto de pares vacíos y `TagError` explícito.

**Resultado:** 4/4 pytest en verde. Resuelto por su cuenta con pistas
conceptuales (niveles 1-2), nunca la solución completa.

## Observaciones
- Fluido con el `for` y las guardas una vez orientado.
- Pidió guía en: qué método separa pares (`split`), cómo agregar a un dict
  (no `append`, sino `d[k]=v`), y orden de guardas + `continue`.
- Dejó vivo el edge de `split("=")` con valores que contienen `=` (no rompe
  los tests). Item de repaso.

## Próximo paso
Seguir en f1.python-profesional: instalar **uv**, usar **ruff** + revisión de
tipos, ver **dataclasses** y refactorizar a un **paquete**. Repasar de entrada
el edge `split("=", 1)` y la distinción de tipos de error.

---

# Sesión 2 — 2026-07-27

- **Tópico:** f1.python-profesional (F1)
- **Duración:** ~30 min
- **Estado final del tópico:** `visto` (falta uv/ruff/type-check/paquete para cerrar).

## Repaso (recuperación activa)
- `split("=", 1)` y valor de las excepciones con nombre: ambas respondidas
  correctamente de memoria. Cabos de S1 interiorizados.

## Qué se vio
`dataclasses`: `@dataclass`, campos con/sin default y su orden, `__post_init__`
para validar al construir, y los dunder generados (`__repr__`, `__eq__` por valor).

## Ejercicio
`ejercicios/fase-1/f1.python-profesional/02-config-dataclass/` — tipo `completar`.
`RunConfig` (dataclass tipada) con validación en `__post_init__` y `ConfigError`.

**Resultado:** 5/5 pytest en verde. Resuelto por su cuenta con pistas 1-2.

## Observaciones
- Campos y orden a la primera; entendió `__repr__`/`__eq__` al preguntarlos.
- Dos bugs propios, encontrados leyendo el output de pytest sin que se le diera
  la solución: (1) `ConfigError(...)` sin `raise`; (2) frontera `<` vs `<=`.
- Buen reflejo de logging: incluyó el valor ofensor en el mensaje de error.

## Infraestructura
- Corregida la identidad de git: config **global** ahora `pliperkiller
  <pip.kro@gmail.com>` (antes `vanesavega`). Reescritos los commits previos a la
  identidad correcta + force-push.

## Próximo paso
Cerrar el tópico: instalar **uv** + **ruff**, revisión de tipos, y refactorizar
`parse_tags` + `RunConfig` a un **paquete** que pase el lint. Al lograrlo, el
tópico pasa a `aprendido`.

---

# Sesión 3 — 2026-07-27  (CIERRE de f1.python-profesional)

- **Tópico:** f1.python-profesional (F1)
- **Duración:** ~30 min
- **Estado final del tópico:** **`aprendido`** (next_review 2026-07-29).

## Setup
- Instalados **uv 0.11.32** y **ruff 0.16.0** (vía `python -m pip install uv ruff`).
  Se invocan con `python -m uv` / `python -m ruff`.

## Repaso (recuperación activa)
- Orden de campos default en dataclass y secuencia `__init__ → __post_init__ →
  raise → instancia al GC`: respondidos (el 1 requirió afinar; el 2 correcto).

## Qué se vio
Por qué existe **uv** (vs pip+venv+requirements), **ruff** como linter, y
**estructura de paquete**: `__init__.py`, imports relativos (`.`), y `__all__`
como API pública. Distinción de los dos tipos de F401 (basura vs re-export).

## Ejercicio
`ejercicios/fase-1/f1.python-profesional/03-paquete-config/` — tipo completar+debug.
Refactor del código de S1+S2 a un paquete `aiconfig` (tags.py, config.py,
__init__.py) que pase ruff y pytest.

**Resultado:** `ruff check .` → "All checks passed!" y **4/4** pytest.
Resuelto por su cuenta con pistas conceptuales (1-2). `__init__.py` idiomático
(imports relativos + `__all__`).

## Observaciones
- Bug propio corregido: self-import circular (`from aiconfig import ...` dentro
  del propio `__init__.py`) → imports relativos.
- Buenas preguntas conceptuales ("¿qué hace `__all__`?").
- **Caveat honesto:** el criterio pide "revisión de tipos"; type-checker
  dedicado (mypy/pyright) aún no corrido. Diferido a f1.apis-rest-fastapi.

## Próximo paso
`posicion_actual = f1.git-flujo`. Además, review de f1.python-profesional el
2026-07-29 (recuperación activa para posible `dominado`).
