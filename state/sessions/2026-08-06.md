# Sesión 11 — 2026-08-06 (iniciada 2026-08-05)

## Repasos (2/2 superados)
- `f1.python-profesional` (dominado, +7): TypeError de defaults mal ordenados **con la regla completa** (el cabo de S4) + F401 dos salidas. SUPERADO → intervalo +21, next_review 2026-08-27. Caveat de type-check cerrado en el ejercicio de hoy.
- `f1.async-basico` (aprendido, +2): pool de conexiones exacta; gather sin `return_exceptions` correcta en lo esencial (precisión dada: la excepción llega a quien hace `await`, no "al loop"). SUPERADO → +7, next_review 2026-08-13.

## Concepto: FastAPI parte 2 — API key + mypy
`APIKeyHeader` (callable, `auto_error=False` → `None`), inyección de dependencias con `Depends`, portero None-check + `compare_digest` (timing attack), key desde `os.environ` con fallo en arranque, `dependencies=[...]` en el decorador. Deuda de mypy cobrada (narrowing de `str | None`).

## Ejercicio: 02-api-key (script + predecir) — RESUELTO por su cuenta (pistas 1-2)
Protegió POST + DELETE nuevo (204/404), GETs públicos. Verificado ejecutando: 8/8 curls (401 sin key / key mala, 201, 204, 404, público), `KeyError` sin env var, mypy limpio, ruff limpio, security scheme en OpenAPI (Authorize en /docs).
- P1: predijo 401; su 1ª versión dio 500 (`compare_digest(None, str)` → TypeError) — lo diagnosticó él mismo al preguntarle. Conexión: es el bug exacto que mypy marca.
- P2: predijo el reciclaje de ids correctamente (gap de S10 CERRADO). Tapó además el 500 del store vacío con `max(keys, default=0)` por iniciativa propia.
- Leyó el código fuente de `APIKeyHeader.__call__` por iniciativa propia.

**Tópico `f1.apis-rest-fastapi`: contenido COMPLETO** (6 endpoints, API key, /docs). Sigue `aprendido`; review 2026-08-08.

## Incidentes
- La demo del concepto no le llegó (texto de chat previo a tool calls invisible para el estudiante, 2ª vez). Regla permanente nueva: **demos siempre como archivo** (`demo_api_key.py` funcionó). Mucho tiempo en preguntas conceptuales sueltas; el ejercicio salió igual.
- Entorno **Windows/PowerShell** detectado (antes Linux): `curl` = alias de `Invoke-WebRequest` → enunciado actualizado con `curl.exe` + escapado `\"` + `$env:`. Los enunciados futuros llevan ambas variantes.

## Limpieza de debilidades (retiradas)
Guard invertido (S7+S11 limpias), sombreado de builtins (S10+S11), diseño de ids (P2 predicha), type-check formal (mypy corrido).

## Próximo paso
- Sesión 12: `f1.testing-pytest` (último tópico de F1). Auditar "por qué pasa este test" (deuda de S7: asserts dentro de `pytest.raises`).
- Reviews pendientes: fastapi 2026-08-08 (candidata: auto_error True/False + compare_digest), git-flujo 2026-08-10 (cerrar criterio: 2º PR con conflicto vía PR), async 2026-08-13.
