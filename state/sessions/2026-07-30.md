# Sesión 7 — 2026-07-30

## Qué se vio
`f1.pydantic-validacion` parte 2: modelos anidados (Forma A objeto vs
Forma B dict crudo y su efecto en `loc`), `model_validator(mode="after")`
para reglas multi-campo, `Field(default_factory=...)`, serialización y
roundtrip (`model_dump_json` / `model_validate_json`). De contrabando:
mecánica básica de pytest (`assert`, `pytest.raises`, `match`, asserts
muertos dentro del with).

## Apertura
- Recuperación activa de la pregunta abierta de S6 (cómo acumula Pydantic
  errores): hipótesis parcial ("holdea la excepción hasta instanciar") →
  corregida a atrapar-y-acumular por campo. Concepto asentado.
- Sin repasos vencidos (git-flujo vence 31/07, python-profesional 05/08).

## Ejercicio y resultado
`ejercicios/fase-1/f1.pydantic-validacion/02-asistente-anidado/` (tipo
script + predicción): 3 modelos anidados con validators y `test_model.py`
propio (esqueleto con 2 ejemplos dado por el tutor al detectar que pytest
como tópico no se había enseñado; los 6 casos restantes escritos por el
estudiante). **8/8 en verde, verificado ejecutando pytest.** P1 (loc de
error anidado) predicha correctamente: `("modelo", "temperatura")`.

Errores cometidos y corregidos por el estudiante (pistas nivel 1-2):
`self.name` vs `self.nombre`, asserts muertos dentro del `with
pytest.raises`, test 5 construyendo el anidado con Forma A (no ejercitaba
la anidación). El patrón de guard invertido NO reapareció (3/3 derechas).

## Estado
- `f1.pydantic-validacion`: visto → **aprendido** (criterio de dominio del
  tópico cumplido: 3+ modelos anidados + validators + tests propios).
  `next_review` 2026-08-01.
- `posicion_actual` → `f1.async-basico`.

## Próximo paso
- Repasos que vencen: `f1.git-flujo` (31/07) — incluye cerrar su criterio
  de dominio (2º PR con conflicto resuelto vía PR); `f1.pydantic-validacion`
  (01/08).
- Deuda de recuperación activa: ¿qué excepción lanza `self.name` cuando el
  campo se llama `nombre`? (AttributeError — cobrar sin material a la vista).
- Tópico nuevo siguiente: `f1.async-basico` (event loop, async/await, httpx).
