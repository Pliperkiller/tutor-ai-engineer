# Sesión 4 — 2026-07-29

- **Tópico:** f1.git-flujo (F1) — parte 1 (flujo local)
- **Duración:** ~35 min
- **Estado final del tópico:** `visto` (falta parte 2: GitHub + PRs)

## Repaso (recuperación activa) — f1.python-profesional
3 preguntas de memoria: orden de campos con default en dataclass ✅,
self-import circular en `__init__.py` → import relativo ✅, y las dos salidas
del F401 (borrar vs `__all__`) ✅ (esta última necesitó re-preguntar, pero la
sacó solo). Único cabo: no nombró el `TypeError` exacto.
**→ `f1.python-profesional` pasa a `dominado`. next_review 2026-08-05 (+7).**

## Qué se vio
Branches como líneas de historia paralelas (y como **punteros** a commits),
`git switch -c` / `git merge`, **fast-forward vs merge commit** (divergencia o
no), conflictos: por qué existen (git compara texto, no significado) y cómo se
resuelven (editar, quitar marcadores, `add` + `commit`; la mezcla es válida).

## Ejercicio
`ejercicios/fase-1/f1.git-flujo/01-branches-conflicto/` — tipo debug+predecir,
sobre un `repo-practica/` generado por `setup.sh` (gitignorado).

**Resultado:** completado por su cuenta. Conflicto resuelto con la mezcla
correcta (opus + temperatura 0.2 + 2048), sin marcadores; branch propia
`docs-uso` con 2 commits atómicos; todas las branches fusionadas.
Predicciones escritas antes de ejecutar en `RESPUESTAS.md`.

## Observaciones
- **Gap del día:** predijo commit de merge donde hubo fast-forward, y tras el
  nudge no lo identificó en su propio graph → se dio la explicación completa
  de "branch = puntero". Luego corrigió su P2 con sus palabras. Por eso el
  tópico queda en `visto` (además de faltar la parte de PRs).
- Dejó el mensaje por defecto en el merge commit del conflicto (el enunciado
  pedía documentar la decisión). Reforzar: el mensaje documenta el porqué.
- Autonomía buena: configuró `core.editor vi`, usó `>>` para anexar el log al
  md, resolvió el conflicto sin pedir la solución (pidió un ejemplo análogo,
  no la respuesta — buen instinto).

## Próximo paso
Sesión 5: **f1.git-flujo parte 2** — publicar un repo en GitHub, branches
remotas, **pull requests** (crear, revisar, fusionar) y un conflicto resuelto
vía PR. Abrir retomando fast-forward vs merge commit como recuperación activa.
Review de f1.python-profesional: 2026-08-05.

---

# Sesión 5 — 2026-07-29 (mismo día, sesión aparte)

- **Tópico:** f1.git-flujo (F1) — parte 2 (remoto GitHub + PRs)
- **Estado final del tópico:** `visto` → **`aprendido`** (next_review 2026-07-31)

## Qué se vio
Remotos (`origin` = alias a URL; `origin/main` como puntero local al estado
remoto), `gh repo create` + `git push -u`, pull requests como pausa de
revisión, `gh pr create` / `gh pr merge --merge`, y por qué GitHub crea
merge commit donde git local haría fast-forward (trazabilidad del PR).

## Incidente formativo
Re-corrió `setup.sh` (que hace `rm -rf repo-practica`) y perdió la historia
local de S4. Se convirtió en la lección central: git solo protege lo que vive
en un `.git` vivo o en un remoto — ni reflog ni pull recuperan un `.git`
borrado. Rehizo merge + conflicto **de memoria** (recuperación activa
implícita): mezcla correcta, sin marcadores, y esta vez documentó el mensaje
del merge (aunque solo 1 de los 3 parámetros de la decisión).

## Ejercicio
`ejercicios/fase-1/f1.git-flujo/02-remoto-pr/` — tipo script+predecir.
**Resultado:** completado por su cuenta (pistas nivel 1-2). Repo
`Pliperkiller/asistente-config` público, branch `docs-parametros` con 2
commits atómicos, PR #1 con body descriptivo, fusionado vía gh (verificado
con `gh pr view`: MERGED, merge commit en el graph). P1 ✓; P2 predijo FF
(razonamiento local correcto); P3 detectó él mismo que GitHub `--merge`
siempre crea merge commit.

## Observaciones
- **Gap de S4 (fast-forward) CERRADO:** lo explicó correctamente 2 veces,
  incluida la precisión "no se crea ningún commit, solo se mueve el puntero".
- **Error nuevo:** committeó en `main` con la branch ya creada → PR "main
  into main". Reparado con guía (ff-merge + `reset --hard origin/main`).
  Vigilar conciencia de HEAD en el próximo trabajo con branches.
- **Entorno:** `gh` de snap falla (`remote-https is not a git command`)
  cuando gh ejecuta git internamente. Workaround: push manual con git, gh
  solo para API. Fix pendiente: gh vía apt.

## Próximo paso
- **Review f1.git-flujo (2026-07-31):** segundo PR sobre `asistente-config`
  con **conflicto resuelto vía PR** → cierra el criterio_dominio (2+ branches
  vía PR, 10+ commits). Preguntar de memoria: ¿dónde vive un PR? ¿por qué
  GitHub no hace FF con --merge?
- **Concepto nuevo siguiente:** `f1.pydantic-validacion` (posicion_actual).
- Review f1.python-profesional: 2026-08-05.

---

# Sesión 6 — 2026-07-29 (tercera del día)

- **Tópico:** f1.pydantic-validacion (F1) — parte 1
- **Estado final del tópico:** `visto` (parte 2 pendiente: anidados +
  model_validator + serialización)

## Qué se vio
Por qué existe Pydantic (validación en la frontera vs `__post_init__`
manual), coerción desde dicts crudos, errores en lote, `Field` con
constraints declarativas y el contrato del `field_validator`
(recibe → raise ValueError → return).

## Ejercicio
`ejercicios/fase-1/f1.pydantic-validacion/01-config-pydantic/` — tipo
**test**: 6 tests fallando como especificación ejecutable; mismo dominio
que el ejercicio de dataclasses (ModelConfig).
**Resultado: 6/6 verificado.** Pistas nivel 1-2 (contrato del validator;
señalar el typo y el guard). Corrigió ambos errores por su cuenta.

## Observaciones
- **Guard invertido, 2ª ocurrencia** (antes `<=` vs `<`): registrado como
  patrón recurrente en debilidades. Instalar hábito de leer la condición
  de rechazo en voz alta.
- Leyó y resolvió solo el `TypeError` del decorador (`modelo=` → `mode=`).
- **Entorno:** terminal de VS Code sin pyenv (`/usr/bin/python3` sin
  pytest). Workaround: ruta completa al python de pyenv. Fix de raíz
  pendiente; uv por proyecto lo eliminará en el capstone.
- Tercera sesión del día — ritmo alto sostenido sin caída de calidad.

## Próximo paso
- Abrir con la **pregunta sembrada**: ¿cómo junta Pydantic 3 errores en un
  ValidationError si el validator hace raise? (respuesta: valida campo por
  campo y recolecta; el raise corta solo ese campo).
- **Parte 2 de pydantic:** modelos anidados + `model_validator` +
  `model_dump`, apuntando al criterio_dominio (3+ modelos anidados).
- Review f1.git-flujo desde el 2026-07-31 (2º PR con conflicto vía PR).
